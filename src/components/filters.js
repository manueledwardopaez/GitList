import styled from 'styled-components'

const FilterStyled = styled.div`
    grid-area: filters;
    background: blue;
`

function Filters() {
    return (
        <FilterStyled>
        Filter
        </FilterStyled>
    )
}

export default Filters
